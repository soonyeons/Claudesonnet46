name: Build and Deploy

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

### **3단계: Commit**

아래에서:
- **Commit message:** `Fix GitHub Actions workflow`
- **"Commit changes"** 클릭

---

### **4단계: 배포 확인**

1. **Actions** 탭 클릭
2. 초록색 ✅ 체크 표시 나타나면 성공!

---

## **✅ 완료되면:**
```
https://soonyeons.github.io/Claudesonnet46/
